% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loading.R
\name{load_criteria}
\alias{load_criteria}
\title{Load criteria definitions.}
\usage{
load_criteria(
  components = NULL,
  load_all = FALSE,
  csv_engine = "read.csv",
  criteria_types = NULL,
  reference_subset = NULL,
  edition = NULL
)
}
\arguments{
\item{components}{A string or character vector of component names. The
return type changes depending on whether a single string or a vector is
provided. Available components: \code{"criteria-thresholds"},
\code{"criteria-descriptions"}, \code{"criteria-variables"}, \code{"criteria-types"},
\code{"reference-data"}, \code{"reference-metadata"}, \code{"sources"}.}

\item{load_all}{Logical. Load all available components. Cannot be combined
with \code{components}.}

\item{csv_engine}{The method for loading CSV files. One of \code{"read.csv"}
(default), \code{"readr"}, or \code{"data.table"}.}

\item{criteria_types}{Character string or vector. When loading
\code{"criteria-thresholds"} or \code{"criteria-descriptions"}, restrict to these
criteria types only. Defaults to all types.}

\item{reference_subset}{Character string or vector. When loading
\code{"reference-data"} or \code{"reference-metadata"}, restrict to these datasets
only. Defaults to all datasets.}

\item{edition}{Character string specifying the edition to load, e.g.
\code{"2026-06-01"}. Defaults to the latest edition.}
}
\value{
The loaded data. A data frame or named list for a single component;
a named list of those when multiple components are requested.
}
\description{
Loads and returns the criteria definitions contained in the package.
}
\examples{
df_thresholds <- load_criteria("criteria-thresholds")
print(df_thresholds)

list_metadata <- load_criteria("criteria-descriptions")
print(list_metadata)

criteria <- load_criteria(c("criteria-thresholds", "criteria-descriptions"))
print(criteria[["criteria-descriptions"]])

}
